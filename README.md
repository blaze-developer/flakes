# BlazeDev's NixOS Flake
This is the repository for my NixOS flake! :3 I think if i keep configuring this right,
it will be possible to pull it down, add a hardware configuration, and recreate the whole
system.